const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },

  extend: {
    colors: {
      black:{
        DEFAULT: '#000000',
        100: '#000319',
      }
    }
  }
};

export default config;
